To run TW12, make sure to install the needed additional packages:
  $ sudo apt update
  $ sudo apt install openjdk-8-jre
 
You can also run this TW on Windows if you have the java runtime installed.
